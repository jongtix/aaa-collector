#!/bin/sh
# Pre-push hook: Tests
# Formatting + Linting → pre-commit, SpotBugs → CI

echo "[pre-push] Running tests..."

if ! ./gradlew test --daemon --quiet; then
    echo "[pre-push] Tests failed. Fix failing tests before pushing."
    exit 1
fi

echo "[pre-push] All tests passed."
